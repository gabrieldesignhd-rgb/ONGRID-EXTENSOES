plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "NoxToons"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"

    source {
        lang = "pt-BR"
        baseUrl = "https://noxmanga.co"
    }
}
